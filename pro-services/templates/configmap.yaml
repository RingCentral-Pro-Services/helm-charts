apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pro-services.fullname" . }}-cfg
  labels:
    {{- include "pro-services.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.config | nindent 2 }}
